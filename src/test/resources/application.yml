spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: test
    password:
    driver-class-name: org.h2.Driver

  h2:
    console:
      enabled: true
      path: /h2
  jpa:
    defer-datasource-initialization: true

jwt:
  jwt-key: ${secret-key}